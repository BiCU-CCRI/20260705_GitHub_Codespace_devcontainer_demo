{
  "name": "Demo 01 - Minimal mamba devcontainer",
  "image": "ghcr.io/rocker-org/devcontainer-features/miniforge:2",
  "postCreateCommand": "mamba install -y -n base -f .devcontainer/environment.yml -c conda-forge -c bioconda && mamba clean --all --yes",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}
