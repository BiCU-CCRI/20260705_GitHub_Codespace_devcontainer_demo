{
  "name": "Demo 03 - Dockerfile-based devcontainer (mamba + git-graph)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "mhutchie.git-graph"
      ]
    }
  }
}
